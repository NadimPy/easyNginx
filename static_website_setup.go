package easynginx

import (
	"os"
	"fmt"
	"log"
	"easynginx"
)

// this source provides the needed functions to setup a static website and calls on the Certbot package for SSL if requested

type website struct {
	domain string
	ssl bool
	
}

func main_website_setup(string domain, ) string {
	log.
	Get_conf_path()
	
}